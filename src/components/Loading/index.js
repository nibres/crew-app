// @flow
import React from 'react';

import { Text, LoadingModal } from './styled';

const Loading = () => (
  <LoadingModal>
    <Text>Loading...</Text>
  </LoadingModal>
);

export default Loading;
